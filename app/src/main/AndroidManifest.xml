<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devmel.apps.serialterminal"
    android:versionCode="2"
    android:versionName="1.1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-feature android:name="android.hardware.usb.host" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateAlwaysHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.devmel.apps.serialterminal.SerialOptions"
            android:label="@string/title_activity_serial_options" >
        </activity>
        <activity
            android:name="com.devmel.apps.serialterminal.PortSelect"
            android:label="@string/title_activity_port_select" >
        </activity>
        <activity
            android:name="com.devmel.apps.serialterminal.LinkbusAdd"
            android:label="@string/title_activity_linkbus_add" >
        </activity>
    </application>

</manifest>
